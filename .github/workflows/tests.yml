name: Tests
permissions:
  contents: read
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    env:
      JWT_SECRET: rilOU1e755Q319XliJ0rpEqNFqkdd5h8yw7I4E7QJVM=
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: backend
      - name: Run tests
        run: npm test
        working-directory: backend
