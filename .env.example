# Database Configuration
# For local development with docker-compose
POSTGRES_USER=books
POSTGRES_PASSWORD=books_password
POSTGRES_DB=books
POSTGRES_PORT=5433

# Database connection string
# Local: postgresql://books:books_password@localhost:5433/books
# Fly.io: Automatically set when you attach the database
DATABASE_URL=postgresql://books:books_password@localhost:5433/books

# Server Configuration
PORT=3000

# JWT Secret for authentication tokens
# Generate a strong secret with: openssl rand -base64 32
# IMPORTANT: Change this in production!
JWT_SECRET=your-secret-key-here-change-in-production

# WebAuthn Configuration
# RP_NAME: Human-readable name shown during passkey registration
RP_NAME=Books Tracker

# RP_ID: Your domain WITHOUT protocol or port
# Local development: localhost
# Production: your-app-name.fly.dev (or your custom domain)
RP_ID=localhost

# RP_ORIGIN: Full URL WITH protocol
# Local development: http://localhost:5173
# Production: https://your-app-name.fly.dev (or your custom domain)
# IMPORTANT: Must match exactly or passkeys won't work!
RP_ORIGIN=http://localhost:5173

# CORS Configuration
# Should match RP_ORIGIN in production
# Local development: http://localhost:5173
# Production: https://your-app-name.fly.dev
CORS_ORIGIN=http://localhost:5173

# Frontend Configuration (for local development only)
# In production with single-app deployment, this is set to empty string
# so API calls are made to the same origin
# Local development: http://localhost:3000
# Production (Fly.io): "" (empty string, set automatically in Dockerfile)
VITE_API_URL=http://localhost:3000

# Optional: Enable database query logging for debugging
# LOG_QUERIES=false
